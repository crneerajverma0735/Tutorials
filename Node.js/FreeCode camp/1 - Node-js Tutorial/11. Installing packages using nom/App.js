// first of all we need to install lodash (npm install lodash)

const _ = require('lodash');

let example = _.fill([1, 2, 3, 4, 5], "banana", 1, 4);

console.log(example);


// if we want to uninstall package (npm uninstall lodash)